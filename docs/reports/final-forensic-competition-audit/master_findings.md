# 10. Master Findings Matrix

| ID | Severity | Category | Component | Finding | Evidence | Impact | Fix | Confidence |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **SEC-001** | P0 | Security | `.env`, `scratch/` | Plaintext Gemini, Groq, and Razorpay API secrets committed/stored. | `.env:13-14`, `scratch/setup_webhook.py:14`, markdown reports. | Total compromise of external accounts. | Revoke and rotate all keys. Purge from Git history if committed. | HIGH |
| **EVAL-001** | P1 | Evaluation | `evaluation/` | 100% synthetic scenarios with rigged baseline guaranteeing 0% natural recovery. | `simulator.py:50`, `evaluator.py:81-85`. | Invalidates "measured recovery" claims; judge will view numbers as fabricated. | Rebalance the generator and baseline logic; use historical probability distributions. | HIGH |
| **AI-001** | P1 | AI | `intelligence/` | Deterministic fallback mimics AI by hardcoding confidence metrics (`0.95`, `0.80`). | `analyzer.py:168-176`. | Undermines "AI-driven" claim; exposes system as a basic rule engine. | Integrate LLM to actually perform Risk Assessment, or correctly brand as "hybrid." | HIGH |
| **UX-001** | P2 | API / UX | `frontend/api/client.ts` | Frontend swallows all 4xx/5xx backend JSON error details. | `client.ts:25-26`. | Critical failure context hidden from user; shows generic errors. | Parse `response.json()` on `!response.ok` and bubble up `detail`. | HIGH |
| **SEC-002** | P2 | Architecture | `scratch/` | Standalone scripts bypass system invariants (e.g. `pay.py`, `setup_webhook.py`). | `scratch/pay.py`, `scratch/setup_webhook.py`. | Direct unauthorized data mutation; demo destabilization. | Quarantine or delete the `scratch/` directory. | HIGH |
| **ARCH-001** | P3 | Architecture | `api/main.py` | Missing tenant isolation on GET endpoints; uses static frontend key. | `api/main.py:461`, no filtering by `merchant_id`. | Contradicts "multi-merchant" README claims. | Update docs to classify project as single-merchant prototype. | HIGH |
| **UX-002** | P3 | UX | `Dashboard.tsx` | Dashboard recalculates backend metrics client-side, bypassing `/analytics`. | `Dashboard.tsx:64-90`. | Brittle UI; duplicates logic. | Map UI directly to `/analytics` aggregate outputs. | HIGH |
| **UX-003** | P3 | UX (a11y) | `CaseDetailView.tsx` | Unlabeled interactive elements (Back button) and non-semantic progress steppers. | `CaseDetailView.tsx:90`, `RecoveryJourney.tsx:28`. | Fails screen-reader accessibility. | Add `aria-label` and `role="list"`. | HIGH |
